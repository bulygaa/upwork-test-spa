import styled from "styled-components";

const Wrapper = styled.div`
  width: 100%;

  min-height: 100vh;
  height: 100%;

  position: relative;
`;

const Header = styled.header`
  width: 100%;
  min-height: 60px;
`;

const S = {
  Wrapper,
  Header,
};

export default S;
