import styled from "styled-components";

const Wrapper = styled.div`
  width: 100%;

  height: 100vh;

  position: relative;
`;

const Header = styled.header`
  width: 100%;
  min-height: 60px;
`;

const S = {
  Wrapper,
  Header,
};

export default S;
